panGenome Part2 pipeline:
本流程是NGS-pangenome 流程的第二部分，改部分流程主要的作用是cluster。
代码基于CPG代码开发，但基本是完全改写。

#####################使用方法###################
--indir
    这里请输入上一步流程的结果的文件夹(e.g./share1/home/kongshuang/20221205.TIB/TIB.batch6.out/pangenomeRes)。
--outdir
    输出结果文件夹，文件夹是否创建好无所谓但是路径末尾不要带斜杠。

--config(optional)
    参数文件夹，如果不指定，则使用默认参数。


######################结果######################
outdir/leftEnding/UpRepCluTot2.fa
outdir/rightEnding/UpRepCluTot2.fa
outdir/bothEnding/UpRepCluTot.fa
outdir/unmapped/UpG38Unmap.fa
MergeLefRig/MergeContain.position
MergeLefRig/MergeOverlap.position
######################整体说明##################
##########流程
这个是流程第二步，我们的输入是第一步的结果文件夹。在我们的流程中，首先是将之前的不同样本的结果合并，放到[Left | Both | Right].ending.bed 文件中，然后我们会根据位置和比对的结果做cluster內的merge，这里我没有控制
阈值，但是经过评估96%的序列都会有short align coverage 大于90%，所以这里我没有设置阈值，如果你想做的更加精细，建议增加一个过滤的步骤。
在这之后我们会做不同的cluster之间的比对，然后我们会做left和right之间的compare。这一步的结果在MergeLefRig文件夹下面。
另外在结果中我们会有两个额外的position的文件， MergeContain.position以及MergeContain.position，这里前者中的序列代表着通过将left和right进行merge，他们转移到了both的目录下，所有两个插入的位点都要统计，但是对于后者
我们的merge的序列被放到了SEP的目录下，所以具体的这个序列对应的插入位点，要根据文件最后一列结果的指示('l':left, 'r':right)来进行判断，并只取一个位点。
##########线程与速度
这里因为在python中使用cmd调用子进程，所以避免了GIL的影响，故因此直接用了threading module来设计多线程而非多进程。全流程中只在filter G38这一步进行了多线程的过滤，默认线程数是10，没有设置外界借口，如果想更改
请直接将代码复制到你的目录下，然后在启动文件: pangenomePart2.py的295行进行更改。
##########其他
MergeLefRig/MergeOverlap.position文件对应的结果在有些情况下会因为popins的调用失败而没有结果，在这种情况下请联系我，我可以手动跑一跑。


######################更新日志##################
1.对于unmap的序列比对到left或者right的情况，我们先不move，我们先对left或者right比对的情况做一个看看情况如何再move。
2.更改了final_ref.py中输出的时候对于之前的AllPlaced的情况下的考虑。过去在输出Updata的reference的时候，我们把能够merge到一起的重复序列，选择一个做为rep然后输出所有相关序列
这里输出的时候会先把rep的序列输出一遍，但是其实是没有必要的，因为在后面输出的时候，我们的rep序列也是包含在contig_cluster[contig]内部的，这里contig_cluster就是我们的All_placed的东西。这里的contig就是我们的rep的contig的名字（更改的点在123行以及后面）
